// This is the environment JS file covering all the Settings.
// You can keep adding here as we go along.
host = "http://localhost:3000/";
namespace = "api/v1/";
loginUrl = host + "/token";

apiUrl = host + namespace; // http://localhost:3000/api/v1/

